<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiAppMinhasCompras.Views.ListaProduto"
             Title="Minhas Compras">

    <Grid RowDefinitions="Auto,Auto,*,Auto" Padding="10">

        <!-- Search bar -->
        <SearchBar x:Name="searchBar"
                   Grid.Row="0"
                   Placeholder="Buscar produto..."
                   SearchButtonPressed="searchBar_SearchButtonPressed"
                   TextChanged="searchBar_TextChanged" />

        <!-- Add button -->
        <Button Grid.Row="1"
                Text="+ Novo Produto"
                BackgroundColor="#512BD4"
                TextColor="White"
                Margin="0,5"
                Clicked="btnNovo_Clicked" />

        <!-- Product list -->
        <CollectionView x:Name="cvProdutos"
                        Grid.Row="2"
                        SelectionMode="Single"
                        SelectionChanged="cvProdutos_SelectionChanged">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <SwipeView>
                        <SwipeView.RightItems>
                            <SwipeItems>
                                <SwipeItem Text="Excluir"
                                           BackgroundColor="Red"
                                           Invoked="SwipeItem_Delete_Invoked"
                                           CommandParameter="{Binding Id}" />
                            </SwipeItems>
                        </SwipeView.RightItems>

                        <Frame Margin="0,4" Padding="12" HasShadow="True" CornerRadius="8">
                            <Grid ColumnDefinitions="*,Auto">
                                <VerticalStackLayout Grid.Column="0">
                                    <Label Text="{Binding Descricao}"
                                           FontSize="18" FontAttributes="Bold" />
                                    <Label FontSize="14" TextColor="Gray">
                                        <Label.FormattedText>
                                            <FormattedString>
                                                <Span Text="{Binding Quantidade, StringFormat='{0:N2}'}" />
                                                <Span Text=" x R$ " />
                                                <Span Text="{Binding Preco, StringFormat='{0:N2}'}" />
                                            </FormattedString>
                                        </Label.FormattedText>
                                    </Label>
                                </VerticalStackLayout>
                                <Label Grid.Column="1"
                                       Text="{Binding Total, StringFormat='R$ {0:N2}'}"
                                       FontSize="18" FontAttributes="Bold"
                                       TextColor="#512BD4"
                                       VerticalOptions="Center" />
                            </Grid>
                        </Frame>
                    </SwipeView>
                </DataTemplate>
            </CollectionView.ItemTemplate>

            <CollectionView.EmptyView>
                <VerticalStackLayout HorizontalOptions="Center" VerticalOptions="Center">
                    <Label Text="🛒" FontSize="60" HorizontalOptions="Center" />
                    <Label Text="Nenhum produto na lista"
                           FontSize="18" TextColor="Gray" HorizontalOptions="Center" />
                    <Label Text="Toque em '+ Novo Produto' para começar"
                           FontSize="14" TextColor="LightGray" HorizontalOptions="Center" />
                </VerticalStackLayout>
            </CollectionView.EmptyView>
        </CollectionView>

        <!-- Total footer -->
        <Border Grid.Row="3" BackgroundColor="#512BD4" Padding="15" StrokeThickness="0" Margin="0,5,0,0">
            <Border.StrokeShape>
                <RoundRectangle CornerRadius="10" />
            </Border.StrokeShape>
            <HorizontalStackLayout HorizontalOptions="Center" Spacing="10">
                <Label Text="Total:" TextColor="White" FontSize="20" FontAttributes="Bold" />
                <Label x:Name="lblTotal" Text="R$ 0,00"
                       TextColor="White" FontSize="20" FontAttributes="Bold" />
            </HorizontalStackLayout>
        </Border>

    </Grid>

</ContentPage>